#ifndef CONSTANTS_H
#define CONSTANTS_H

const int FPS = 60;
const int MILLISECS_PER_FRAME = 1000 / FPS; // сколько миллисекунд должно занимать завершение каждого кадра в секунде

const int PIXELS_PER_METER = 50; // 50 pixel in meter

#endif